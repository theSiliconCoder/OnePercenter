
q325 update to ema scalping 
// vps pass: KCnR*R?aP8Ru)BE9.sNLYhMk8K@RcsXU



next steps: @ October 30
// purchase 2.5k/5k acct
// add notifications for trades and tp/sl
// deploy on cloud 
// topup usd card 

next steps: post 1st payout
// research and implement algorithm to detect strong moves aligned with tp and trail
// wait till next ema alignment cycle before reentry or one trade a day  
// try ways to improve accuracy: BO confirmation, other indicator    
// also enter on 5/50 crossing not just 5/9
------------------------------------------------------------
issues:
// avoid news hrs, ranging markets 
// make tp and sl responsive to market regime 
// find a way  to maximize wins
// use sweeplevel as stoploss if less than maxrisk/working risk 


solved // breakoutpt persisting for too long
// sweeps are only detected in killzones : fine

done // max one profitable trade per KillZone per day 
solved // not buying in NY on 02 may
solved // selling without breakout on may 2, NY
done // breakPoint should remain unchanged except a opposing candle is detected when waiting for BO 
// use validation checklist for BO for easier debuging



todo:
done // int GetCurrentHour()
// KillZone LN, NY <- checkActiveKillZone()
// SessionParams <- getSessionData(SessionCode AS,LN)

// checkLowestLow(now and AsianEndTime(not-inclusive)) : Low 

*// checkForBreakout(Bullish BO)
 - are we in KillZone? yes 
 - is bar 1 bullish? yes 
 - price > bar 1 high? yes return true


// SignalGen (TrendBias UPTREND, DOWNTREND) - only called in KillZones


#UPTREND
- checkActiveKillZone : LN
- getSessionData(AS) : AS-Low 
- checkLowestLow(now and AsianEndTime(not-inclusive)) : Low 
- if LowestLow < AS-Low 
   - if checkForBreakout(Bullish BO) == true 
      - fire buy signal, supply LowestLow as stop 


#DOWNTREND
- checkActiveKillZone : LN
- getSessionData(AS) : AS-High 
- checkHghestHigh(now and AsianEndTime(not-inclusive)) : High 
- if HighestHigh > AS-High 
   - if checkForBreakout(Bearish BO) == true 
      - fire sell signal, supply HighestHigh as stop 


// placeOrder(sl)
- given: riskMargin, RR, leverage, balance
- calculate position size
- place market order 
   

video links:
https://www.youtube.com/watch?v=aTMFouI9mF4  manipulation: low risk 
https://www.youtube.com/watch?v=IUo5AwmsE9A umar achraf, trading psyco, skill dev
https://www.youtube.com/watch?v=yLuH8YZXORQ









LIQUIDITY SWEEP STRATEGY DEFINITION: 

#Chart/TimeFrame: 15m

1. DailyBiasAnalysis: Use 50 EMA and Top Down Analysis to Predict Today's Market Direction/Trend Strength, Accept only 80% Probability and Above Average Trend Strength
   - Manual: Use Top Down Analysis: Consider 50EMA, Reversal Points, OBs, FVGs, Candle Patterns and TrendLine on across multiple timeframes
   - Auto: +/- slope of 5ema on daily chart + price above/below 50ema line + filter on 30m + if price is within line + filter band, where is price coming from and 
            heading to? 
   - Hybrid: Do manual Prediction at Night and post to bot, if no manual prediction, bot defaults to auto .

2. SignalGen Opportunities: Timebased LS in asian, london, ny or 1wk, 1d etc. 

   TimeBased Liquidity Sweep (LS) of S/R Level with market gearing up for movt in direction of bias : Asian 8H (22 - 5) and London 8H (6-12) High and Low

   Killzones: london (6-11am), ny (12-5pm) 

   Homogeneous 5m BO in killzone preceded by ->
     case 1: 15m {swinglevel below/above} prev swinglevel 
     case 2: 15m {swing touch} of prev swinglevel

   AlgoDef:
   // predict today's bias
   -> ontick: 
   // within killzone?: no -> return
      yes: on 5m chart check candle 0,1 for breakout based on bias 
         breakout?: no -> return
      yes: record the last two swing pts between curr 15m bar and 14 bars behind
          // first bar lower/higher (bias dependent) than second and position == 0: fire original signal
          // if killzone is london use smart fixed tp, if ny use ema based tp 


3. TradeManagement 2025 second half:
  - SL: Size position according to 0.5% of acct balance, per position
      - Risk of (10 - 15 pips) must always engulf sl, swing pt and entry, otherwise join at neck and use max riskmargin of 15
      - If several swing pts within 10pips range, choose farthest swing pt for sl
  - BE: Trigger @ 1:1 or 1.5 or None depending on volatility
  - TP: Trigger @ 1:2 then exit at close of tp trigger candle for london and by 5 and 9 EMA meeting for newyork.  
      - exit short: 5 > 9, exit long: 9 > 5
  - Top Up: New Signal after existing position is in BE, max 2 positions   

  - Max 3 original trades per day only all around LN and NY Session.

4. Rules
a. swing pt > riskmargin? -> use riskmargin 
b. fail once, reduce risk by 50%









=========================

EMA STRATEGY DEFINITION: 

#Chart/TimeFrame: 30m, 4h

1. SignalGen Opportunities: Timebased london, ny and 1d. 

   Killzones: london (6-11am), ny (12-5pm) 

   AlgoDef for Entry Signal Gen:
   -> ontick: 
   // in position?: yes -> return 
   // within killzone?: no -> return
      yes: on main chart, check for ema crossings 
         - validate signal via checklist:
            - ema crosses and stays crossed for 2 bars of lower tf  
            - signal count in killzone == 0
            - ema 5 above/below current candle open/close : hold
            - ema 5 slope +ve for buy, -ve for sell : hold
            - deduce last swinglevel for sl : use fixed riskmargin 1atr 
            - enter on Homogeneous BO on lower timeframe : hold 

   AlgoDef for Exit Signal Gen:
   -> onnewbar: 
   // in position?: no -> return
      yes: on main chart, check for ema crossings 
         - validate signal via checklist:
            - ema crosses and stays crossed for 2 bars of lower tf 
            - set positionClosed flag: resets start of next session/day 
            


2. TradeManagement 2025 second half:
  - SL: Size position according to 0.5% of acct balance, per position
      - Risk of (15 pips) must always engulf sl, swing pt and entry, otherwise join at neck and use max riskmargin of 15
      - If several swing pts within 10pips range, choose farthest swing pt for sl
  - BE: Trigger @ 1:1 Atr or None depending on volatility
  - TP: Trigger @ 1:2 Atr then exit at close of tp trigger candle for london and by 5 and 9 EMA meeting for newyork.  
  - Hybrid-TP: Always enter two positions, Trigger TP1 @ 1:2 Atr - wait for tp bar to close, set pos2 to BE, then exit position 2 by ema crossing. 

  - Max 1 original trades per day only all around LN and NY Session.

3. Rules
a. swing pt > riskmargin? -> use riskmargin 
b. 